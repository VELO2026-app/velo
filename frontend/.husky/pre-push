# Гейт перед пушем (до CI — локальная истина): type-aware линт с бюджетом,
# юнит-тесты, прод-сборка (внутри build — vue-tsc). Красный пуш не уезжает.
cd frontend && pnpm lint && pnpm test && pnpm build
