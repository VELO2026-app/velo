# VELO — DELIBERATELY NOT FIXED (reference; moved off the boot path 2026-08-16)

> **Owner-ruled 2026-08-16.** This list lived in `Agent-Velo/DS-build-plan.md` section C until the board
> hit its 60,000-byte ceiling five times in one session. It is REFERENCE, not live work: every entry
> exists to stop someone "fixing" something that was left alone ON PURPOSE, and the person about to do
> that is already opening the file the entry names — a pointer reaches them in time.
>
> ⚠ **WHAT DID NOT MOVE, and the distinction is the whole ruling: the four prohibitions that have
> actually bitten repeatedly stay ON the board** — `STILL LOAD-BEARING`, items 1-4: the repo test idiom,
> BG-ROOT, the cancelled robot/E2E, and SW1's attendance lock. Those meet a cold agent at boot rather
> than behind a link, because a prohibition read after the fact is a prohibition that failed.
>
> ⚠ **READ THIS BEFORE "FINISHING" ANYTHING THAT LOOKS HALF-DONE IN THESE AREAS.** An entry here is a
> DECISION, not a backlog item. Two kinds live here and both stay: an owner ruling that something is
> accepted as-is, and a REFUTED hypothesis someone already chased down — the second is banked precisely
> so it is not chased twice.

---

> ⚠ **THE FIVE BELOW WERE RESCUED 2026-08-15 FROM REGISTRY ENTRIES THE OWNER RULED OUT OF THE ROADMAP** — their only remaining step was "somebody must look", which is the testers' job now. **The entries went; these could not, because a tester report does not regenerate a decision.** Measurements: `velo/docs/device-verification-findings.md`.
- **The master's attended-only student list stays attended-only** — `№609` stands; it is NOT unified with the wider four-source display list. Do not "finish" that.
- **The master's bell does not act on `action_data`** — a tap marks read and stops. Deliberate for v1, stated in the view header and covered by a test.
- **One Zoom Pro host runs ONE meeting at a time** — a SECOND licence is needed from the moment two masters run overlapping practices. A purchasing consequence, not a defect.
- **The ~15-minute attendance verdict lag is a deliberate cost** of the Zoom API route rather than the SDK. Do not re-file it as slowness.
- **`ZoomRegistrant.last_sync_error` is written and read by nobody** — the same blindness `PK-Z2` fixed one level up, left out of scope on purpose.
- **Two admin reports count BOOKINGS, not the attendance verdict** (`admin/stats/overview_service.py:190-258`, `admin/metrics/service.py:145-167`) — intentionally: they answer the question "how many signed up", while the verdict lags ~15 minutes. With Zoom's arrival the motive STRENGTHENED.
- **Four redundant guards, not bugs:** the `stepNext` guard against its own `:disabled` · the conjunct `loading && items.length===0` (unreachable) · the double `busyId` (`onApprove`+`doApprove`) · the `distributionTotal===0` clause in `bucketPct` (`VRatingBar.hasValue` itself cuts `NaN`). The fourth confirmed by reasoning, not by a test.
- **`hasParsedMethods` is always true** (`AdminMasterReviewView.vue:478-480`) — the verbatim fallback is dead, the user does not see it; do not touch the 1148-line screen for dead code.
- **The silent `load()` catch in `AdminCatalogView`** and **`AdminParticipantsView`, which cannot distinguish `ApiResponseError` in principle** — diverge from the other admin lists, left deliberately.
- **`MasterNotificationsView`'s 4 UNBACKED TOGGLES — OWNER RULED 2026-08-16, option C: VISIBLE, DISABLED, with a short coming-soon marker.** 5 of 9 keys persist via comms; `new_checkin`/`new_feedback`/`ai_summary`/`monthly_report` have no category, so `onToggle` flips the visible switch and returns before any call — which is exactly why it reads as saved. ⚠ **HIS STANDARD, kept because it governs more than this screen: I argued four dead rows among five live ones is a screen half made of promises; his answer is that the app is still being built, forthcoming controls on screen are normal, and a finished-product bar is not one he has set. My `D` was "delete them" and he overruled it.** Not made to persist: that needs our own storage for a preference nothing consumes, and hits the no-migration constraint.
- **The master zone is covered 18/24 BY the owner's DECISION** (4 low-return + 2 static stubs) — do not "finish closing" for a round number.
- **`AdminCatalogView` will show masters' personal directions indistinguishably from global ones** (found 07-22 while building personal methods) — cosmetics on a trusted admin surface.
- **V4 "the admin does not see the personal method in the mixed case" — PROVEN FALSE (Navigator-34 + Orc-69, `№571`). Do NOT re-file.** Unrecognized strings land in `unmatched`, and `AdminMastersView.vue:155-157` renders them as a `customText` chip ALONGSIDE the recognized directions. The registry entry had read only the fallback at `:144` and missed `:155-157`; its claim that `parsed.unmatched` "is read nowhere" was literally true and irrelevant, since `customText` carries it. **The class is the lesson: "verified personally" can be false, and OPENING the file is what caught it.**
- **🔐 "MinIO + nginx basic-auth share one password" — REFUTED FOR VELO, measured 2026-07-25. Do NOT re-file.** VELO has no MinIO and no nginx basic-auth at all: four containers only, zero tracked mentions of `minio`/`htpasswd`, no `auth_basic` block anywhere. Postgres and Redis secrets come from two independent `openssl rand` calls and are never offered to an override prompt, so the AIVIS failure mode has no analogue here. ⚠ STATED LIMIT: this proves the installer-generated state and the ABSENCE of those services — never what a human may have set by hand on a live box. ⚠ The string Agent-One chased is the backend dev's GitHub fork URL in `claude-memory/velo_deploy_workflow.md` and BELONGS there — do not scrub it. **The lesson outliving the finding: it was entered as a registry item on another agent's report WITHOUT measuring, then withdrawn — [[velo_premise_check]] in its purest form, on a security item.**
- **B10 "bot name from the token" — REJECTED EXPLICITLY (Navigator-34 + Orc-70, №573), closing by the condition "rejected explicitly".** `config.py` instantiates `Settings()` on import, the header (`config.py:28-29`) records: the module is imported by Alembic BEFORE the application starts ⇒ it must be network-free. A live `getMe` in the settings validator would block the backend start, the migrations and pytest collection, and would fail on the dev fake token. The name is ALREADY derived via `getMe` — correctly, once at provisioning (`install_velo.sh:538-551`). Do NOT re-file.
- **The layout-overflow blind spot is FORMALLY ACCEPTED, not built (owner ruled option A, `№587`; carried here from the retired B9).** happy-dom returns zero layout metrics, so a scroll-width-vs-client-width check inside our Vitest suite would be VACUOUS — the only real detector is a browser, and the owner cancelled the browser robot 07-16. The existing net (testers caught both historical catalogue slips) is accepted as sufficient. **RE-OPEN ONLY IF overflow keeps recurring — and then the cheap add is a STATIC guard (flag fixed-px widths on flex children / missing `min-width:0`), NOT a runtime browser check.**
- **HIDING ZOOM'S DOWNLOAD BUTTON IS COSMETIC, NOT A LOCK — recon `№625`, 2026-07-29. Do NOT re-file it as a bug and do NOT promise a user that recordings cannot be downloaded.** Why the switch looks like a lock: it does remove the button. But the video must stream to the viewer's browser to be watchable at all, so a fetchable URL is on the page regardless, and the passcode gates the PAGE, not what a browser does with a video it has already loaded. Anyone holding the link can take the file. Preventing it needs streaming through our own server instead of a Zoom link — a different architecture, not a setting.
- **The Zoom app's admin scopes are DELIBERATELY BROAD — owner ruled 2026-07-29, RETIRING B19, which asked to narrow them.** Measured in the Marketplace console that day on the S2S app `VELO Attendance` (Account-level, not published): **113 scopes selected**, the owner's own "Select All (Admin)". His words: we use the ones we need and do not use the rest. **Do NOT re-file this as a defect and do NOT narrow them.** ✅ Both recording scopes ARE offerable to a Server-to-Server app (measured in the console 2026-07-29 against four web sources that said otherwise) — no second app type is needed. *(Detail in this file's git log; it is reference, not a prohibition.)*
- **The peach warning panel uses the EXISTING `--velo-warning-bg`, not a new token (Navigator call, made not asked, `№611`).** The owner's SVG draws `--velo-peach-300` at ~30% over white; the DS ships `--velo-warning-bg` at 40%. The difference is imperceptible and minting a near-duplicate would violate the standing "do not burn variants" rule, so the existing token was used. **Do not "correct" this to match the SVG literally** — the reference says WHICH token to use, it does not license minting one. Revisit only if it reads wrong on a device.
- **The two surviving attendance knobs stay ENV-ONLY (retired AT-4, closed 2026-07-24; the guard is what survives).** `zoom_report_ripen_margin_minutes` (15) and `zoom_attendance_decision_deadline_minutes` (120) are configured in the server `.env` and nowhere else, because the project has no admin-settings mechanism at all and building one for two numbers is not warranted. **RE-OPEN ONLY IF a second attendance parameter accumulates AND an admin surface is built for the whole set — never for one number.**
