# Карточка managed agent — шаблон

Заполняется по п. 5.2, 5.6, 5.7 ТЗ фабрики (`docs/ai-dev-factory-tz.md`). Заполняет агент, утверждает владелец. Незаполненное помечается явно — пустых полей нет.

---

# <Имя агента>

- **Статус:** черновик / пилот / боевой
- **Репозиторий / место работы:** <repo, окружения>
- **Владелец:** <человек, который принимает результат и отвечает за него>

## Тип работы (workflow)

<Какой тип работы агент выполняет и какая стадия конвейера (п. 4.1–4.7 ТЗ) ему принадлежит. Один тип работы = одна карточка.>

## Модели

| Назначение | Модель | Fallback |
| --- | --- | --- |
| <основной workflow> | <модель> | <модель> |

<Смена модели — только через eval-гейт (п. 5.6); начиная с Фазы 2 назначение фиксируется в `models.yaml` (п. 10.3.1 ТЗ).>

## Инструменты и права (least privilege)

<Явный список: инструмент → что именно разрешено. «Нет права в карточке — нет права в токене» (п. 5.7). Запись во внешние системы — только через человеческий гейт, где применимо. Доступа к production-инфраструктуре у агентов нет (п. 8.5.1).>

## Источники контекста

<Где агент берёт контекст: репо, Linear, доки, дашборды. Инструменты подключаются лениво — каталог tools не грузится в контекст целиком (п. 3.4 ТЗ).>

## Evals (п. 5.6)

- **Набор:** <какие evals, где лежат>
- **Когда гоняются:** при любой смене модели, промпта или источника контекста — блокирующий гейт изменения
- **Пороги:** <что считается провалом>

## Правила эскалации к человеку

<Контракт (п. 10.2 ТЗ): агент приходит с вопросом и предложенным ответом (варианты и цена каждого), никогда с открытым вопросом. Что считается эскалацией для этого агента.>

## Лимиты затрат

<Spend-cap на сессию/задачу. До Фазы 5 допускается «не заданы», с датой пересмотра.>

## Стоимость (вручную до Фазы 5, п. 8.5.4)

<Замер или явное «не замерено»; чем замерено.>

## Метрика: cost per useful outcome

- **Useful outcome для этого агента:** <что считается принятым полезным результатом>
- **Текущее значение:** <число или «не считается до Фазы 5»; как считается>
