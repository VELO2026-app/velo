# Трекер фаз фабрики (п. 8.4 ТЗ)

> Linear F-тикеты отложены по решению владельца 2026-09-17 — фазы трекаются здесь; когда появятся F-тикеты, файл остаётся сводкой. Правило миграции (п. 8.5.2): фаза не расширяется на новый workflow, пока предыдущая не прошла свой гейт; провал гейта — откат пилота, а не снижение порога.

| Фаза | Содержание | Гейт | Статус |
| --- | --- | --- | --- |
| 0 — правила игры | `docs/agents/` + шаблон карточки + карточка velo_pm_bot + TTM-базлайн + этот трекер | — (несколько дней; единственный риск — «заведём попозже») | ✅ 2026-09-17 |
| 1 — детерминированный контур (1–2 нед) | CI на PR в `test`: pytest, vitest, vue-tsc, eslint, prettier, drift-check `generated.ts` (регенерация в CI + `git diff --exit-code`) | красный PR не мержится; человек в ревью перестаёт ловить то, что ловит машина | 🔄 2026-09-18: контур поднят и стабильно зелёный на `ai-dev-factory` (`.github/workflows/ci.yml`): frontend-гейт (lint, prettier, vitest, vue-tsc build) + drift-check `generated.ts`. Гейт уже отработал по назначению: поймал непроформатированный `transcription.test.ts`, приехавший из `test` без контура. Ветка синхронизирована с `test` (мерж `51514daa`, включая BE-34 — FE-68 разблокирована). Осталось два шага: (а) backend-джоба — владелец бэка (первый прогон показал 383 ruff-находок свежим ruff из заявленного в pyproject диапазона — сначала сверить версию линтера); (б) branch protection «красный PR не мержится» — при возвращении `test` |
| 2 — пилот review-агента (2–4 нед) | доступы и секреты по п. 5.7 → агент по чеклисту `VELO-Anti-Patterns.md` → evals на исторических PR → режим «только комменты» | ловит ≥80% известных паттернов при шуме ≤1 ложного замечания на PR; только после гейта — обязательный контур PR | ⬜ |
| 3 — формализация (параллельно, ~1 нед) | а) карточка `verify-frontend-ui`; б) шаблон «Linear → ТЗ»; в) карта компонентов + `design.rules.md` (11.2); г) чистка компонентной базы (11.6): дедуп Create/EditPractice, гекс→токены, перенос V-компонентов `shared/`→`ui/`, тесты топ-10 примитивов | артефакты утверждены владельцем | 🔄 2026-09-18: в) сделано — `frontend/design/component-map.md` (37 DS-компонентов + 81 иконка; Figma-колонка ждёт экспортов Уровня 0) и `frontend/design/design.rules.md`; оба — на утверждении владельцем. а), б) не начаты; г) не начата (тесты у 4/37 примитивов, 14 гексов в 12 вью, V-компоненты в `shared/`) |
| 4 — Context Graph MVP (3–4 нед) | индекс + поиск естественным языком по `docs/`, `VELO-*.md`, Linear, `generated.ts`, миграциям, тестам; граф ссылается на первоисточник | на 10 закрытых тикетах агент называет правильные файлы за ≤N ходов (N замерить до/после) | ⬜ |
| 5 — экономический контур (2 нед) | счётчики затрат по сессиям, spend tiers с approval flow, дашборд | по каждому запущенному агенту известен cost per useful outcome | ⬜ |
| 6 — раскатка конвейера (квартал) | стадии 1–3: draft ТЗ из Linear + независимая проверка + UI-варианты по карте; 4: implementation milestone-ами с CI; 5: tgqa между CI и приёмкой; 6: smoke в `velo update`; 7: дежурный агент-аудиты | по стадиям | ⬜ |

## Параллельный трек: tgqa-engine (раздел 9)

DevEx-фича, отдельный репозиторий (velo — READ-ONLY; конфиг, сценарии и baseline — `frontend/visual/`). Запуск P0 заблокирован двумя решениями владельца: хранилище baseline-картинок (п. 9.2 п. 2; рекомендация — git-lfs) и приоритет относительно P3 curator-groups (п. 9.2 п. 5).

## Ночабельные задачи (п. 10.4)

Пока Фазы 1–2 не закрыты, ночная смена доступна: карта компонентов из барелов (11.6.1), `design.rules.md`, шаблон «Linear → ТЗ», переезд V-компонентов в `ui/`, гекс→токены (значение 1:1), тесты топ-10 примитивов, черновик CI-workflows Фазы 1 на ветке. Нельзя на ночь: дедуп Create/EditPractice и тяжёлых вью (дневная задача с продуктовыми суждениями), запись в Linear, деплой, миграции, контракты, мажоры зависимостей, генерация UI-вариантов до tgqa-оракула.

## Хроника запуска

- **2026-09-17 — Фаза 0 ✅ и Фаза 1 🔄 за один день.** Ветка `ai-dev-factory` создана (test временно недоступен); `docs/agents/` заведён: правило входа агентов, шаблон карточки, первая карточка velo_pm_bot; TTM-базлайн замерен (медиана ≈ 4.7 дн по 16 репрезентативным тикетам FE+BE); docs/ почищен (−4 мёртвых дока, добавлен индекс). CI поднят и доведён до зелёного: pnpm 12.4.2 запинен явно (action искал `packageManager` в корне), установка зависимостей повторяет паттерн Dockerfile (пустой wheel из pyproject одного, `app` через PYTHONPATH), backend-джоба снята — бэк вне этого контура. Linear синхронизирован: FE-71/FE-65/FE-73 закрыты как отгруженные.
- **2026-09-17 — FE-66 прошла через полный контур.** «Квартал» на дашборде мастера: план в карточке → код (3 файла) → узкий тест 62/62 → сюита 2824 → CI зелёный. Первая фича конвейера; Done — после приёмки на устройстве.
- **2026-09-18 — `test` подлит в ветку, гейт поймал первый чужой дрейф.** Мерж `51514daa`: GT-41 (транскрибация на бэкенд, ключ OpenRouter ушёл из бандла; ТЗ голосового ввода удалено как устаревшее), BE-34 (периодная аналитика → FE-68 разблокирована), BE-37, BE-38, GT-37/38. CI отловил непроформатированный `transcription.test.ts` из test — починено, прогон зелёный. Попутно удалена случайная корневая заглушка `package.json`.
- **Ожидают владельца:** приёмка FE-66 и FE-75 на устройстве; решение PM-9 (разблокирует FE-74/BE-37); B35-развилка (см. `docs/primer-b35-doctor-gate.md`); tgqa — хранилище baseline и приоритет относительно P3.
- **Снято с очереди:** «11 Dependabot-уязвимостей» — эхо до FE-73: все 11 npm/фронт, фактические версии в lockfile вне уязвимых диапазонов, алерты ждут пересканирования.
