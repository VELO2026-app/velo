# Owner-parked roadmap items — do NOT build

> **Moved out of `Agent-Velo/DS-build-plan.md` 2026-08-15 by Navigator-46**, under that file's own de-ballast rule: the two levers left are removing what is CLOSED and moving REFERENCE into `velo/docs/`. **These are OWNER-PARKED (`FLEET-CONVENTIONS §3.6`) and ONLY THE OWNER UN-PARKS THEM.** The board keeps the prohibition in one line each; the REASONS live here, and the reasons are the point — several exist to stop a rebuild of something that already ships.
> ⚠ **READ THIS BEFORE BUILDING ANYTHING THAT SOUNDS LIKE ONE OF THEM.** Nothing here has a closing condition; a parked item is not open work.

---

## The parked set
- **🅿 PARKED BY THE OWNER 2026-07-27 — consent before a master adds a person to a group (Q14).** `add_group_member` today takes ANY user id, no relationship required. Measured NOT a leak: every profile aggregate is scoped to that master's own practices, so a stranger yields zeros; only name + avatar show, which the member list already did. ⚠ **READ BEFORE BUILDING, or you will rebuild what exists:** reusable GROUP invite links already ship (`createGroupInvite` + `GroupJoinView`) and already give consent-based joining OF ONE GROUP. The missing half is a PERSON-level relationship that survives across groups. Revisit with Zod's notifications module.
- **🅿 PARKED BY THE OWNER 2026-07-29 — a MASTER cannot watch the recording of their own practice (`REC-1` tail).** Not an oversight: the meeting runs under the S2S app's own Zoom user, so the master has no path through us AND none through Zoom itself. Owner: "if it is needed later we will add it; there is no point now." ⚠ The backend half is one line — `get_booking_recording` (`bookings/service.py:1327-1355`) grants owner-only where the neighbouring `get_booking_by_id` already grants owner-OR-master. What is actually missing is a SCREEN in the master zone, which is why this was deferred. **CLOSE: the owner un-parks it and a master-side surface is designed.**
- **🅿 PARKED BY THE OWNER 2026-07-27 — structured report reasons + admin stats.** `Report.reason` is ONE `Text` column and `G12`'s multi-select JOINS the chosen reasons into it, so they are readable but not countable or filterable. The owner accepted that and parked the proper version — real data plus an admin surface that counts and filters — to be done with the backend. ⚠ Do NOT half-build it as a side effect of a frontend task: the string-join in `ReportUserSheet` is the deliberate interim, not a bug.
- **Server push of the role change — do NOT build:** notifications come from Zod; our own sockets would duplicate his work.
- **An admin integrity panel — TWO DIFFERENT BEASTS, don't mix:** (a) entering secrets via the UI — expensive, no settings mechanism exists at all, a build from scratch; (b) a read-only liveness light for integrations (Zoom · payments · bot · DB) — far cheaper, and it would answer "are the permissions granted" without a trip to the server.
- **Migration to the Zoom SDK** — when there are many users; for now we pay with the verdict lag.
- **🅿 PARKED BY THE OWNER 2026-07-30 - dependency vulnerability coverage (`SEC-1`), measured at `№632`, not started.** TWO different things, do not merge them. **(a) The frontend alarm is FALSE:** GitHub's push banner says 29 vulnerabilities, the Dependabot API returns 7, ALL `auto_dismissed`, ZERO open - all npm, all `frontend/package-lock.json`, all `scope: development`, three packages, none imported in `frontend/src`. Build tooling that never reaches a user; auto-dismissed by GitHub's own npm-devDependency rule. The banner's 29 reconciles with nothing the API can return. **(b) THE REAL GAP is the runtime side:** `backend/pyproject.toml` carries only range specifiers and the repo has NO lock file of any kind, so all 17 pypi packages - `fastapi`, `stripe`, `sqlalchemy`, `aiogram`, `asyncpg` among them - have NO resolved version to compare against an advisory. **The payment / database / bot side of the product has ZERO vulnerability monitoring and reports nothing, silently**; `dependabot_security_updates` is disabled, so no fix PR would open either. Navigator's D, unruled: commit a lock file for scanning WITHOUT switching the deploy to install from it, then decide. **CLOSE: the backend has a lock file GitHub can scan, or the owner rules the blind spot accepted with its reason recorded.** (Per-alert detail and the 29-vs-7 forensics: this file's git log.)
