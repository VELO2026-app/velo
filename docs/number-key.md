# VELO — historical number key

> **Moved out of `Agent-Velo/DS-build-plan.md` 2026-08-15 by Navigator-46**, under that file's own de-ballast rule: the only levers left are removing what is CLOSED and moving REFERENCE into `velo/docs/`. This is a LOOKUP TABLE consulted rarely and carried on the every-boot path for no reason. **Nothing here is open work.**
> ⚠ It is kept so nobody RE-USES a number, not because the meanings are all verified — the caveats below are part of the record and travel with it.

---

## Number key (do not expand without need, a collision already happened)

> **RECONCILED 2026-07-28 (Navigator-37). This section was NARROWER than the key this home hands to its orchestrators** — it carried only the `T21`/`OWNER`/`T22` families while the handoff directive carried six more. Two of our own artifacts disagreeing, and the one the orc gets was the complete one. The full key is below; each family carries where it is attested, because a key nobody can verify is how a number gets re-invented.

`T21-1` Zoom link unreachable from the interface · `T21-2` chips beyond the block boundary · `T21-3` sideways drift · `T21-4` refusal screen · `T21-5` master-mode button · `T21-6` directions filter · `T21-9` "+ Add" in the catalog · `T21-10` `custom_` chips · `OWNER-1` no one to start the meeting · `OWNER-2` view not confirmed · `OWNER-3` series not created · `T22-1` tab bar on the review screen · `T22-3`/`T22-5` practice-list tabs · `T22-4` series cancellation (closed by measurement) · `T22-6` master's personal method.

`T23-1` merged Zoom button · `T23-2` onboarding scroll · `T23-3` template flood. ⚠ **Attested ONLY in the Navigator-36 handoff directive** — `T23-1` appears once on this board (in the A1-bis removal list) and `T23` occurs ZERO times in the whole `velo` git log, so `T23-2`/`T23-3` have no artifact behind them beyond that directive. Carried so nobody re-uses the numbers; do not treat the meanings as measured.

`V1`-`V10` the adversarial-validation findings of `№566/567` (verified complete: `V1` still open in section B, `V4` refuted in section C, the other eight retired deploy-closed 2026-07-24) · `SW1`-`SW17` the full-code correctness sweep of `№576` (**upper bound verified in the `velo` log — `3369b45` "period-range stepper label pinned to UTC (SW17)"**; this board itself names only SW1/2/6/12/15/16, so the range is wider than the board and the log is the source) · `AT-1`..`AT-4` the attendance findings of the 🎯 decision block · `MIG1` the hardening that makes the V7 unique-index migration dedup BEFORE `create_index` · `G0`-`G21` the master-role fix batch of 2026-07-27, section G.

⚠ `T21-7`/`T21-8` in Orc-65's commits mean NOT what's on the board (collision 07-21, history is not rewritten).

